import { Component } from '@angular/core';

/** A shimmering placeholder block. Size it with utility classes, e.g.
 *  <app-skeleton class="h-6 w-40" />. Themes itself via the shared tokens. */
@Component({
  selector: 'app-skeleton',
  template: '',
  styleUrl: './skeleton.css',
})
export class Skeleton {}